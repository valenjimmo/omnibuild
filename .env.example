NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server only. Used solely to deliver invitations after membership authorization.
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional absolute Omnibuild origin when the contractor showcase is deployed separately.
NEXT_PUBLIC_OMNIBUILD_URL=
# Meta inbound webhook integration (server only); no outbound messages are sent by this implementation.
WHATSAPP_APP_SECRET=
WHATSAPP_VERIFY_TOKEN=
